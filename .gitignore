# build output
snapos_ui
snapos_ui.aarch64
*.o
*.aarch64

# release zips
dist/

# downloaded official Libretro release inputs (run knulli/setup-link-cores.sh)
vendor/link-cores/*.so
vendor/link-cores/*.zip

# local dev state (created at runtime under the repo dir on desktop builds)
settings.cfg
activity.dat
sessions.log
scrape_status.txt
ra_check.json*
*.log
__pycache__/

# secrets / personal
*.key
thegamesdb.key
_thegamesdb.key
_VERSION

# local test roms / scraped art / music — never commit
roms/
boxart/
music/
promo/

# editor / OS cruft
*.swp
*~
.DS_Store
*:Zone.Identifier

# personal device list for knulli/deploy.sh -- never publish addresses
knulli/devices.local

# desktop dev binary (BUILD.md builds ./snapos_ui; some setups name it .desktop)
snapos_ui.desktop
