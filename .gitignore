# build output
snapos_ui
snapos_ui.aarch64
*.o
*.aarch64

# release zips
dist/

# downloaded official Libretro release inputs (run knulli/setup-link-cores.sh)
vendor/link-cores/*.so
vendor/link-cores/*.zip

# local dev state (created at runtime under the repo dir on desktop builds)
settings.cfg
friends.cfg
friends.cfg.tmp
player-id.txt
widget-places.cfg
widget-places.cfg.tmp
widget-local-city.txt
battery-prompt.cfg
battery-prompt.cfg.tmp
battery-prompt.cfg.tmp.*
fish-helpers.cfg
activity.dat
sessions.log
scrape_status.txt
ra_check.json*
*.log
__pycache__/

# secrets / personal
*.key
thegamesdb.key
_thegamesdb.key
_VERSION

# local test roms / scraped art / music — never commit
roms/
boxart/
music/
promo/

# editor / OS cruft
*.swp
*~
.DS_Store
*:Zone.Identifier

# personal device list for knulli/deploy.sh -- never publish addresses
knulli/devices.local
# local assistant handoff notes (device addresses and credentials)
CLAUDE_HANDOFF.txt

# desktop dev binary (BUILD.md builds ./snapos_ui; some setups name it .desktop)
snapos_ui.desktop
tests/output/
